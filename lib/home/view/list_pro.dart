import 'package:flutter/material.dart';

class ListPro extends StatelessWidget {
  const ListPro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView.builder(itemBuilder: (context, index) {
      return ListTile();
    });
  }
}
